name: Release packages - next

on:
  workflow_dispatch:
  push:
    branches: [master]

jobs:
  release:
    uses: ./.github/workflows/_omnipackage.yml
    secrets: inherit
    with:
      repository: "Cloudflare R2 next"