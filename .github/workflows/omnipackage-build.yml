name: OmniPackage build agent

on:
  workflow_dispatch:
  # push:
  #  branches: [ main ]

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: omnipackage/omnipackage-agent-ruby/.github/action@master
        env:
          APIKEY: ${{ secrets.OMNIPACKAGE_AGENT_APIKEY }}
